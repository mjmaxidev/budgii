name: CI

on:
  push:
    branches:
      - '**'
  pull_request:

jobs:
  frontend:
    name: Frontend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format:check

      - name: Build
        run: npm run build

      - name: Route smoke test
        run: npm run smoke:routes

  backend:
    name: Backend
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: budgii
          POSTGRES_PASSWORD: budgii
          POSTGRES_DB: budgii
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U budgii -d budgii"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    defaults:
      run:
        working-directory: budgii-api

    env:
      APP_ENV: development
      APP_DEBUG: 'true'
      DATABASE_URL: postgresql+asyncpg://budgii:budgii@localhost:5432/budgii
      JWT_SECRET: ci-dev-secret-change-me
      RECEIPT_STORAGE_PATH: /tmp/budgii-receipts
      RECEIPT_OCR_PROVIDER: deterministic
      INVITE_EMAIL_PROVIDER: log

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: budgii-api/requirements.txt

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Ruff lint
        run: ruff check .

      - name: Ruff format check
        run: ruff format --check .

      - name: Run migrations
        run: alembic upgrade head

      - name: Test
        run: python -m pytest
