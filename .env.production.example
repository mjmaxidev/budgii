# Frontend production build
VITE_API_ENABLED=true

# Hosted Budgii API. Keep the /v1 suffix because the frontend client expects it.
VITE_API_BASE_URL=https://api.budgii.app/v1

# Keep prototype/demo reset tools hidden for production users.
VITE_SHOW_DEMO_TOOLS=false
